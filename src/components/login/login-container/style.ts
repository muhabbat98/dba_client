import styled from 'styled-components';

export const LoginContainerWrapper = styled.div`
  background: white;
  padding: 40px;
  border-radius: 16px;
  width: 430px;
  position: relative;

  & > svg {
    position: absolute;
    top: 28px;
    right: 28px;
    cursor: pointer;
  }
`;
